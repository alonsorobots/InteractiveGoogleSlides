# Core tooling uses only the Python standard library (3.10+).
# The only optional dependency is for splitting an exported PDF into slide
# images (build_deck.py with a manifest "pdf" field). If you export per-slide
# PNGs instead, you need nothing here.
pymupdf>=1.24
